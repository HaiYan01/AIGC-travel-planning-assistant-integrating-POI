# Docker 部署环境变量
# 使用方法:复制本文件为 docker/.env 后填写(请勿提交真实 .env)

# MySQL root 密码(容器首次启动时生效)
DB_PASSWORD=API_KEY

# JWT 签名密钥(建议:openssl rand -hex 32)
JWT_SECRET=API_KEY

# AI 大模型(OpenAI 兼容接口)
AI_API_KEY=API_KEY
AI_API_BASE_URL=https://open.bigmodel.cn/api/paas/v4
AI_MODEL=glm-4-flash
